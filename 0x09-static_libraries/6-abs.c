/**
* _abs- function name
*
* Description: Computes the absolulte value of
* a given input
*
* @n: number to convert
* Return: true (0) or false (1) or (-1);
*/

int _abs(int n)
{
	if (n < 0)
		return (n * -1);
	else
		return (n);
}
